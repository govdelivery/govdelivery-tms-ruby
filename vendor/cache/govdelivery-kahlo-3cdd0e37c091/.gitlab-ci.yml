before_script:
  - ruby -v
  - which ruby
  - gem install bundler --no-ri --no-rdoc
  - export JRUBY_OPTS='-X-C -J-Xmx1024m -J-XX:+UseConcMarkSweepGC'
  - bundle install --jobs $(nproc) "${FLAGS[@]}"

rspec:
  script:
    - bundle exec rake test