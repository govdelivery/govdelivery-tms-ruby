################################################################################
# ATTENTION!
#
# Do not edit this file directly. To edit this file, create a new file and move
# it over this one.
#
# For example:
#
# $ cp config/throttle_email_sending.yml /tmp/x.yml
# $ vi /tmp/x.yml # make your changes
# $ mv /tmp/x.yml config/throttle_email_sending.yml
################################################################################

# NOTE: this is per Sidekiq server!
messages_per_minute: 100

# Throttle messages that match one or more hashes in the array. For example,
# the following would match only messages with the subject of "Throttle me!"
# AND a user_id of 10000:
#
# message_matchers: [{"subject": "Throttle me!", "user_id": 10000}]
message_matchers: []
